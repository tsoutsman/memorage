cargo-features = ["strip"]

[workspace]
members = ["soter-core", "soter-cs", "soter-client", "soter-server", "soter-cert", "soter-stun"]

[profile.release]
strip = "symbols"
lto = "fat"
codegen-units = 1
